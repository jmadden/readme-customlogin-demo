# .env.example
# Copy this file to a `.env` file and replace the placeholder values with your own.
# Do NOT commit your actual `.env` file to version control.

# JWT secret used by ReadMe authentication.
# Generate a secure random string for production.
README_JWT_SECRET=changeme

# Base URL for your ReadMe Hub.
# Example: https://docs.yourcompany.com/
README_HUB_URL=https://your-hub-url.example.com/